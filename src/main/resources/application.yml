spring:
  profiles.active: default
  data:
    mongodb:
      host: localhost:27017
      port: 27017
      database: chatapp
  rabbitmq:
    host:
    port: 0
server:
  port: 9091

---

spring:
  profiles.active: test
  data:
      mongodb:
        host: localhost
        port: 27017
        database: chatapptest
