spring:
  profiles.active: default
  data:
    mongodb:
      host: localhost
      port: 27017
      database: chatapp
server:
  port: 9091

---

spring:
  profiles.active: test
  data:
      mongodb:
        host: localhost
        port: 27017
        database: chatapptest